using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BugsTier1 : MonoBehaviour
{
    // Remove /* and */ to start the task
    /*
    public GameObject door;
    public Transform playerTransform;

    void Start()
    {
        // 1. Unassigned GameObject
        door.SetActive(false); // 👈 NullRef if 'door' isn’t assigned

        // 2. Missing reference to Transform
        Debug.Log("Player position: " + playerTransform.position); // 👈 Will crash if playerTransform not set

        // 3. Uninitialized List
        inventory.Add("Potion"); // 👈 Will crash — list is null

        // 4. Index out of range
        string[] greetings = { "Hi" };
        Debug.Log(greetings[2]); // 👈 Index out of bounds
    }
    */
}
